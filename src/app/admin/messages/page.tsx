/**
 * Admin-scoped messages.
 *
 * Same messaging UI as the member route, but under `/admin` so staff keep the
 * admin sidebar (AdminShell) instead of switching to the member dashboard shell
 * when they open Messages from the admin nav.
 */
export { default } from '@/app/(dashboard)/messages/page'
